# powerline

Installs powerline-status via pipx and injects additional packages into the same environment.

## Variables

| Variable | Default | Description |
|---|---|---|
| `powerline_package` | `powerline-status` | Base pipx package to install |
| `powerline_inject_packages` | `[powerline-gitstatus]` | Packages to inject into the powerline pipx environment |
